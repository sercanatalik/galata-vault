# dist (https://github.com/axodotdev/cargo-dist): the gv, gv-server and gv-mcp
# release binaries, built by .github/workflows/release.yml when a version tag
# is pushed (`scripts/release.sh` makes the tag; RELEASING.md). Regenerate the
# workflow with `dist generate` after changing this file; CI's `plan` step
# fails if the two disagree.
#
# Per package (their Cargo.toml `[package.metadata.dist]`): crates/gv builds
# with `ui`; crates/gv-server and crates/gv-mcp build for Linux and macOS
# only. These three carry `publish = false` -- the one published crate is
# galata-vault, the library they wrap -- so dist is told to build them
# explicitly.

[workspace]
members = ["cargo:."]
# The binaries live in unpublished crates, so name them.
packages = ["gv", "gv-server", "gv-mcp"]

[dist]
cargo-dist-version = "0.32.0"
ci = "github"
# gv on Linux (x86_64, aarch64), macOS (aarch64, x86_64) and Windows (x86_64).
# Every target builds natively on a hosted runner: no zigbuild or
# cross, which cargo-auditable does not support.
targets = [
    "aarch64-apple-darwin",
    "x86_64-apple-darwin",
    "aarch64-unknown-linux-gnu",
    "x86_64-unknown-linux-gnu",
    "x86_64-pc-windows-msvc",
]
installers = ["shell", "powershell"]
# No Homebrew for 0.1.0: the tap repository does not exist, and a release that
# advertises `brew install` and then fails its publish job is worse than one
# that does not offer it. To add it back, create the tap and its
# HOMEBREW_TAP_TOKEN (contents: write, that repository only), then restore:
#
#   installers = ["shell", "powershell", "homebrew"]
#   tap = "sercanatalik/homebrew-tap"
#   publish-jobs = ["homebrew"]
#
# and regenerate release.yml with `dist generate`. The install lines in
# README.md comes back at the same time.
# Provenance: a GitHub artifact attestation for every artifact, and the
# dependency list embedded in every binary (`cargo audit bin` reads it).
github-attestations = true
cargo-auditable = true
install-path = "CARGO_HOME"
install-updater = false
# Pull requests only plan a release; nothing is built or uploaded for them.
pr-run-mode = "plan"

[dist.github-custom-runners]
aarch64-apple-darwin = "macos-14"
x86_64-apple-darwin = "macos-15-intel"
aarch64-unknown-linux-gnu = "ubuntu-24.04-arm"
x86_64-unknown-linux-gnu = "ubuntu-22.04"
x86_64-pc-windows-msvc = "windows-2022"
